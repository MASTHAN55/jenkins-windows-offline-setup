Download maven, java(11-or-17) any version.
create jenkins folder for store the artifacts.
set path in envirorment variables add path and system variables.
search in beroswer `(http://localhost:8080/login?from=%2F or localhost:8080)` for login jenkins.
got to security in jenkins set TCP port RANDOM.
go to jenkins create node.
copy and pste the links in jenkins (cmd) in file manager.
create pipeline, build.





